/** @type {import('next').NextConfig} */
const nextConfig = {
  // standalone output für kleineres Docker-Image — in Phase 8 aktivieren,
  // wenn Prisma-Setup vollständig getestet ist.
  // output: 'standalone',
}

export default nextConfig
